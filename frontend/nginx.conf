server {
    listen 80;
    server_name _;

    root /usr/share/nginx/html;
    index index.html;

    # Single-page app: unknown routes fall back to index.html so React Router works.
    location / {
        try_files $uri $uri/ /index.html;
    }
}
